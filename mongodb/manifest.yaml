apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongodb
  namespace: roboshop
  # these are replica set labels
  labels:
    project: roboshop
    component: mongodb
    tier: db
spec:
  replicas: 1
  selector:
    # these labels are used to select pods
    matchLabels:
      project: roboshop
      component: mongodb
      tier: db
  # pod definition
  template:
    metadata:
      labels:
        project: roboshop
        component: mongodb
        tier: db
    spec:
      containers:
      - name: mongodb
        image: rakesh2218/mongodb:v1
        readinessProbe:
          tcpSocket:
            port: 27017
          initialDelaySeconds: 10
          periodSeconds: 10
        livenessProbe:
          tcpSocket:
            port: 27017
          initialDelaySeconds: 5
          periodSeconds: 5

# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name : mongodb
#   namespace: roboshop
#   lables:
#     project: roboshop
#     component: mongodb
#     tier: db
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       project: roboshop
#       component: mongodb
#       tier: db
# template:
#   metadata:
#     lables:
#       project: roboshop
#       component: mongodb
#       tier: db
#   spec:
#     containers:
#       name: momgodb
#       image: rakesh2218/mongodb:v1
